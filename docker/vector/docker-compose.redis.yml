version: '3.8'
services:
  redis:
    image: redis/redis-stack:latest
    container_name: codex-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
      - "8001:8001"
    volumes:
      - redis-data:/data
volumes:
  redis-data:
